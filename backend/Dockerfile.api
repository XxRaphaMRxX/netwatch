FROM python:3.11-slim

RUN pip install --no-cache-dir \
    fastapi \
    uvicorn[standard] \
    psycopg2-binary \
    pydantic \
    bcrypt \
    reportlab

WORKDIR /app
COPY . .

EXPOSE 8010
CMD ["uvicorn", "api:app", "--host", "0.0.0.0", "--port", "8010", "--workers", "1"]
